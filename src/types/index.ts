export type NavItem = {
  href: string;
  label: string;
};

export type PhilosophyCard = {
  title: string;
  description: string;
};

export type ServiceItem = {
  title: string;
  body: string;
};
